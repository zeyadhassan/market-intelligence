"""Source adapters and the canonical document boundary.

Everything downstream of this package speaks only CanonicalDocument.
Vendor-specific field names must never escape this package (invariant 1).
"""
