[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fi-intel"
version = "0.1.0"
description = "Incremental market-intelligence project scaffold"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fi-intel = "fi_intel.cli:main"

[tool.setuptools.packages.find]
include = ["fi_intel*"]
